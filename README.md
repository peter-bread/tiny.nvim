# tiny.nvim

Minimal config for Neovim.

It currently targets 0.13.

It's very easy to install more plugins than you actually need. This config is
meant to only have "essential" features.

---

I am also slowly developing a `vim.pack` wrapper plugin in this config, but
whether that ever releases is yet to be seen.
